{
  "name": "tdd-spin-bike-power",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "NODE_ENV=test node --test",
    "test:watch": "NODE_ENV=test node --test --watch",
    "start": "parcel index.html",
    "start:test": "NODE_ENV=test parcel index.html",
    "build": "rm -rf dist/ && parcel build index.html --public-url ./ && npm run build:pwa",
    "build:pwa": "node build-sw.js",
    "test:e2e": "NODE_ENV=test playwright test",
    "test:e2e:ui": "NODE_ENV=test playwright test --ui"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.16.0",
    "@parcel/transformer-webmanifest": "^2.16.0",
    "@playwright/test": "^1.56.1",
    "jsdom": "^27.0.1",
    "parcel": "^2.16.0",
    "vitest": "^4.0.4"
  }
}